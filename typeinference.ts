// Type Inference is a powerful feature in TypeScript that allows the compiler to automatically determine the type of a variable based on its initial value. This can help reduce the amount of code you need to write and make your code more concise. However, it can also lead to errors if you try to assign a value of a different type to a variable that has already been inferred.
let n = 200;
n = "two hundred";

let productTitle = "HP Pavillion i5 Core 16 GB Laptop"; // literals
productTitle = 10998;

let isMember = true;
isMember = "No"

